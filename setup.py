from setuptools import find_packages, setup

setup(
    name="axone",
    version="0.0.0",
    packages=find_packages(),
    install_requires=[],
    entry_points={},
    package_data={
        "axone": ["*.py"],
    },
)
